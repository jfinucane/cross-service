<%= form_for(@autocompletion) do |f| %>
  <% if @autocompletion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@autocompletion.errors.count, "error") %> prohibited this autocompletion from being saved:</h2>

      <ul>
      <% @autocompletion.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :prefix %><br />
    <%= f.text_area :prefix %>
  </div>
  <div class="field">
    <%= f.label :words %><br />
    <%= f.text_area :words %>
  </div>
  <div class="field">
    <%= f.label :dictionary_id %><br />
    <%= f.number_field :dictionary_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
